#version 460

layout(local_size_x = 30, local_size_y = 1) in;

layout(binding = 0) buffer output_buffer {
	uint output_data[];
};


void main() {
	ivec2 coord = ivec2(gl_GlobalInvocationID.xy);
	output_data[coord.x] = coord.x;
}
